# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# API Endpoints
GET    /receptors                                                     controllers.ReceptorsAndPdbs.receptorAndPdbIndex()	
GET    /profiles/lId/:lId                                             controllers.Profiles.profileByLigandId(lId)
POST   /profile/prediction/lId/:lId/rName/:rName/rPdbCode/:rPdbCode   controllers.Profiles.predict(lId, rName, rPdbCode)		
POST   /profiles/lId/:lId/rName/:rName/rPdbCode/:rPdbCode             controllers.Profiles.dockAndSave(lId, rName, rPdbCode)
# Home page
GET    /                                                              controllers.Profiles.welcome()

# Map static resources from the /public folder to the /assets URL path
GET    /assets/*file                                                  controllers.Assets.at(path="/public", file)
