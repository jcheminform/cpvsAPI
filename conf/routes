# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# API Endpoints
GET	   /ligands														  controllers.Ligands.ligandSample()
GET    /receptors                                                     controllers.ReceptorsAndPdbs.receptorAndPdbIndex()		
GET    /ligands/:lId                                             controllers.Profiles.profileByLigandId(lId)
POST   /ligands/:lId/rName/:rName/rPdbCode/:rPdbCode   controllers.Profiles.predict(lId, rName, rPdbCode)		
POST   /scoreByLid/lId/:lId/rName/:rName/rPdbCode/:rPdbCode             controllers.Profiles.dockAndSave(lId, rName, rPdbCode)
# Home page
GET    /                                                              controllers.Profiles.welcome()

# Map static resources from the /public folder to the /assets URL path
GET    /assets/*file                                                  controllers.Assets.at(path="/public", file)
