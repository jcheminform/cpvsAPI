# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# API Endpoints
GET	   /sample														  controllers.Ligands.ligandSample()
GET    /receptors                                                     controllers.ReceptorsAndPdbs.receptorAndPdbIndex()		
GET    /profile/lId/:lId                                             controllers.Profiles.profileByLigandId(lId)
POST   /predict/lId/:lId/rName/:rName/rPdbCode/:rPdbCode   controllers.Profiles.predict(lId, rName, rPdbCode)		
POST   /dock/lId/:lId/rName/:rName/rPdbCode/:rPdbCode             controllers.Profiles.dockAndSave(lId, rName, rPdbCode)
# Home page
GET    /                                                              controllers.Profiles.welcome()

# Map static resources from the /public folder to the /assets URL path
GET    /assets/*file                                                  controllers.Assets.at(path="/public", file)
